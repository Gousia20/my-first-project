pipeline {
  agent any
  stages {
    stage ('build') {
      steps {
        echo "aplication build"
      }
    }

    stage ('test') {
      steps {
        echo 'testing the application')
      }
    }
  }
}
